import 'package:flutter/material.dart';

final borderRadius = BorderRadius.circular(12.0);
const edgeInsets = EdgeInsets.all(12.0);
const edgeInsetsHorizontal = EdgeInsets.only(top: 12.0, bottom: 12.0);
const edgeInsetsVertical = EdgeInsets.only(left: 12.0, right: 12.0);
